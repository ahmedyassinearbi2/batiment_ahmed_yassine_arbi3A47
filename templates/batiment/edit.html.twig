{% extends 'base.html.twig' %}

{% block title %}Modifier un bâtiment{% endblock %}

{% block body %}
<h1>Modifier le bâtiment "{{ batiment.nom }}"</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-2">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('batiment_index') }}" class="btn btn-secondary mt-2">Retour à la liste</a>
{% endblock %}
